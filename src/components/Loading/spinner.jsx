import React from 'react';
import './Loading'

const Spinner = () => {
    return (
        <div id="loader" className="loading">
            <span class="loader"></span>
        </div>
    );
};

export default Spinner;