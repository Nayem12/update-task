import React from 'react';
import './Spinner'

const Spinner = () => {
    return (
        <div id="loader" className="loading">
            <span class="loader"></span>
        </div>
    );
};

export default Spinner;